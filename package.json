{
    "name": "webapp",
    "version": "0.0.1",
    "description": "",
    "main": "src/index.js",
    "scripts": {
        "build": "rm -rf dist && webpack --mode=production",
        "bundle": "./bundle.sh",
        "deploy": "./deploy.sh",
        "watch": "watch 'npm run build && npm run bundle && npm run deploy' src/"
    },
    "author": "",
    "license": "",
    "devDependencies": {
        "@babel/core": "^7.4.3",
        "@babel/preset-env": "^7.4.3",
        "@babel/preset-react": "^7.0.0",
        "babel-loader": "^8.0.5",
        "watch": "^1.0.2",
        "webpack": "^4.30.0",
        "webpack-cli": "^3.3.0"
    },
    "dependencies": {
        "react": "^16.8.6"
    }
}
