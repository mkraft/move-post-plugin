{
    "id": "com.mattermost.move-post-plugin",
    "name": "Move Post",
    "version": "",
    "webapp": {
        "bundle_path": "main.js"
    },
    "settings_schema": {
        "settings": [
            {
                "key": "move_behaviour",
                "display_name": "Original Post Behaviour",
                "type": "dropdown",
                "help_text": "What to do with the original post that was moved: delete it or replace it with a string using the replacement template.",
                "default": "",
                "options": [
                    {
                        "display_name": "Delete it",
                        "value": "delete"
                    },
                    {
                        "display_name": "Replace it with string",
                        "value": "template"
                    }
                ]
            },
            {
                "key": "edit_template",
                "display_name": "Replacement Template",
                "type": "text",
                "help_text": "The template to be used when replacing the original post. Markdown may be used just like a normal post and `{{Permalink}}` and `{{NewPostID}}` template variables are interpolated.",
                "placeholder": "[<moved>]({{Permalink}})",
                "default": "[<moved>]({{Permalink}})"
            }
        ]
    }
}